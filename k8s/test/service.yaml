apiVersion: v1
kind: Service
metadata:
  name: trainee-service-test
  namespace: test
spec:
  selector:
    app: trainee
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30002
  type: NodePort
